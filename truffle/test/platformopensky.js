const PlatformOpenSky = artifacts.require("PlatformOpenSky");

contract('PlatformOpenSky', () => {
  it('should read newly written values', async() => {
    const PlatformOpenSkyInstance = await PlatformOpenSky.deployed();
    
    //TODO::Implement unit tests
  });
});
